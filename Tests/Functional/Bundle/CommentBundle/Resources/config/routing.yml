test_thread_inline:
    path: /inline/{id}
    defaults:
        _controller: CommentBundle:CommentController:inlineAction

test_thread_async:
    path: /async/{id}
    defaults:
        _controller: CommentBundle:CommentController:asyncAction
